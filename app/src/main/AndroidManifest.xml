<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fitness_app">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PerryWolfeWeightTrackingApp">
        <activity android:name="com.fitness_app.activities.FoodActivity" />
        <activity android:name="com.fitness_app.activities.DietActivity" />
        <activity android:name="com.fitness_app.activities.ExerciseActivity" />
        <activity android:name="com.fitness_app.activities.UserProfileActivity" />
        <activity android:name="com.fitness_app.activities.NewUserActivity" />
        <activity android:name="com.fitness_app.activities.WeightActivity" />
        <activity android:name="com.fitness_app.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>